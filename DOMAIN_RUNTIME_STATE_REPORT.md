# Domain Runtime State Report

| State | Live result |
|---|---|
| Zone reference | Configured zone id `b50196aa3c373872d7ba23861e29cac1`; Wrangler routing operations succeeded. |
| Email routing | Enabled; catch-all targets Worker `cloud-mail`. |
| Domain ownership table | No `hengmao.org` row. |
| Readiness snapshot | No `hengmao.org` row. |
| Capability state | No `hengmao.org` row. |
| Security policy | No `hengmao.org` row. |
| Runtime API discovery | `domainManaged:false` for `bill@hengmao.org`. |

Decision: **NO BASELINE FREEZE**. Runtime routing exists, but application domain state is not provisioned/linked.
