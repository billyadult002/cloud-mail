# Login Workspace Resolution

`GET /v3/workspaces/resolve` creates/resolves the authenticated user's default workspace, membership role, and capability set. It does not expose another tenant's workspace.
