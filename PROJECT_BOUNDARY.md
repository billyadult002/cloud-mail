# NEXORA Project Boundary

Canonical source-of-truth: `/Users/billtin/Documents/cloudmail`

NEXORA owns CloudMail/GlassMail compatibility assets, Worker and D1 migrations, provider authority, mailbox and conversation systems, Apple signing, `app.wangbei8554.pingguo736`, real-iPhone acceptance, and Movoto sender-bulk history.

FASTONE V23/V24, Workspace/Project inventory, Collaborative Harness OS, `hermes-console`, and `v12.fastonegroup.com` Mission/checkpoint state must not be active NEXORA project state.

Relocated historical snapshots are preserved under `artifacts/project-separation/` and must not be treated as competing canonical source roots.

Run the read-only guard before a Mission verdict:

```bash
python3 scripts/project_boundary_guard.py
```

Guard failure blocks Mission PASS. The guard never deletes or moves files.
