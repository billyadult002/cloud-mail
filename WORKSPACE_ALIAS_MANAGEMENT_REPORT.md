# Workspace Alias Management

Workspace aliases have workspace ownership, identity/mailbox bindings, provider, lifecycle state, and a unique alias address. Provider-side alias creation remains gated on provider authority.
