# Workspace OS Architecture

GPT68 adds `workspaces`, membership, domain, identity, mailbox, alias, provider-grant, provisioning-job, and audit tables. Domain ownership is workspace-scoped; legacy rows are projected without deletion.
