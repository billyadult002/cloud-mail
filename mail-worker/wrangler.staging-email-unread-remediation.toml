name = "cloud-mail-staging-email-unread-remediation"
main = "src/index.js"
compatibility_date = "2025-09-01"

# This configuration is intentionally pinned to the staging D1 database. It is
# not a Worker deployment configuration and must never be repointed.
[[d1_databases]]
binding = "db"
database_name = "cloud-mail-staging"
database_id = "acf160ae-4efd-48d0-9d1b-7500f4cd0f41"
migrations_dir = "remediations/staging-email-unread"
