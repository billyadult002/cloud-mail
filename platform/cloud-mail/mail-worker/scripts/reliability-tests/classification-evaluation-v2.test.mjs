import{describe,expect,it}from'vitest';import cases from'../fixtures/classification-eval-v2.mjs';import{evaluateV2}from'../../src/service/classification-evaluation-v2.js';
describe('classification evaluation v2',()=>{it('covers layered safety and lifecycle candidate scenarios without claiming human gold',()=>{const m=evaluateV2(cases);expect(m).toMatchObject({dataset_version:'classification-eval-v2',cases:16,label_sources:{deterministic_fixture:8,synthetic_adversarial:8}});expect(m.limitations).toContain('No human gold cases in v2 yet');expect(m.release_gates).toMatchObject({runtime_measurements:'not_measured',passed:false});});it('accepts release gates only when runtime counters are explicitly measured',()=>{const runtime=Object.fromEntries(['false_active_commitment','duplicate_active_commitment','duplicate_durable_mission','evidence_insufficient_auto_activation','stale_worker_terminal_write_accepted','provider_writes','tool_calls','external_communications'].map(key=>[key,0]));const g=evaluateV2(cases,runtime).release_gates;expect(g).toMatchObject({...runtime,security_false_negative:0,unsafe_mission_creation:0,automated_origin_mission_creation:0,passed:true});});it('reports each layer separately and marks insufficient samples honestly',()=>{const m=evaluateV2(cases);for(const key of ['security','identity','origin','intent','relationship','event','actionability','category','overlay','ranking'])expect(m).toHaveProperty(key);expect(m.relationship.status).toBe('insufficient_sample');expect(m.category.macro_f1).toBeNull();});});
