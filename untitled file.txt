我先把这个任务拆清楚，然后尽量把能做的真东西做出来交给你。先做几个关键调研（确保我给你的代码和方案是基于 2026 年当前现实，而不是过时信息），再动手写完整的 App 工程。On-device 这块完全可行。现在查证关键的那一点——ChatGPT / Gemini「用登录而非 API key」到底现在能不能合规实现。ChatGPT 的「Sign in with ChatGPT」是真实存在的 OAuth 流程（登录即获得额度，调用走 token 而非手贴 key）——这正好对应你说的「用登录不用 API key」。再确认 Gemini 侧，然后我去看你 cloud-mail 后端的 API 形状，保证联网代码写对。关键发现已经清楚了，足够我做出**诚实且不会害你**的架构决定。最后一步：看一下你 cloud-mail 后端的 API 路由，保证 App 的联网层对得上。后端是 Hono 框架的 Worker。我直接去看前端调用的 API 路径文件，拿到准确的接口形状。我有联网的命令行环境，直接拉取仓库源码来看准确的 API 定义。关键细节确认：`Authorization` 头是**原始 token**（没有 `Bearer ` 前缀），响应包是 `{code, message, data}`。最后看一眼邮件列表返回的字段，然后我就开始写 App。后端 API 的形状已经完全摸清（`Authorization` 用原始 token、响应包 `{code,message,data}`、`/login`、`/account/list`、`/email/list` 等接口与字段都确认了）。三个 AI 路径的现实情况也查清了。

现在我开始把 App 真正写出来。先建工程骨架和最核心的数据/网络/AI 层。Now the core files. Starting with the app entry point and data models:Networking and secure storage:Now the AI layer — the heart of the request. Provider protocol plus the three backends (Apple on-device, ChatGPT, Gemini):ChatGPT provider via official "Sign in with ChatGPT" OAuth (login, not pasted key). I'm keeping the sign-in flow real but clearly marking the one-time registration it needs:Gemini provider — here I have to be straight with you in the code itself about what Google does and doesn't allow:Now the central app state that ties backend + AI together:The `markedRead` re-encode trick is ugly. Let me simplify by making the field mutable instead:Now the UI. Liquid Glass components first:Main inbox screen: